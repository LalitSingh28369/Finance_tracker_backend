spring.application.name=finance-tracker
#-----------------------------------

# ?????????????????????????????????????????????
# application.properties
#
# All configuration lives here.
# For local development use these values.
# For production, use environment variables.
# ?????????????????????????????????????????????

# ?? Server ??????????????????????????????????
server.port=8080

# ?? PostgreSQL Database ??????????????????????
# You created a database called "financedb"

spring.datasource.url=jdbc:postgresql://reseau.proxy.rlwy.net:16200/railway
spring.datasource.username=postgres
spring.datasource.password=OfSAbMTvxLpNkRtXlRdRniCfVSTciERR

# Fix timezone
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JPA ? auto creates/updates tables
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ?? JWT ??????????????????????????????????????
# Change this to any long random string in production!

jwt.secret=mySecretKeyForFinanceTrackerAppVeryLongString2024abcdefgh
jwt.expiration=86400000

spring.main.allow-circular-references=false

# SSL required by Railway
spring.datasource.hikari.data-source-properties.sslmode=require

# ?? Email (Gmail) ????????????????????????????
# Use App Password from Google Account settings
# Google Account ? Security ? 2FA ? App Passwords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=yourgmail@gmail.com
spring.mail.password=your_app_password_here
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ?? Budget Limits (per category per month) ???
budget.limit.Food=5000
budget.limit.Transport=3000
budget.limit.Entertainment=2000
budget.limit.Shopping=4000
budget.limit.Health=2000
budget.limit.Other=1000

spring.mail.username=financetracker.lalit@gmail.com
spring.mail.password=fxzgnlroujgpsyrv


spring.mail.host=smtp.gmail.com
spring.mail.port=587

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000